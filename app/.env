# Mero Calendar — rc.8 config.
# VITE_APPLICATION_ID is optional: when empty, the app resolves its own
# application id from the node by matching the package below.
VITE_APPLICATION_ID=
VITE_APPLICATION_PACKAGE=com.calimero.merocalendar
